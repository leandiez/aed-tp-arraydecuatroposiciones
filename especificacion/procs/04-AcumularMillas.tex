\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage[left=2.5cm,top=3cm,right=2.5cm,bottom=3cm,bindingoffset=0.5cm]{geometry}
\usepackage{AEDLogica, AEDEspecificacion, AEDTADs}
\usepackage{caratula}

\newcommand{\Tipo}[1]{\mathsf{#1}}
% la sintaxis es \newcommand{\nombreDeLaMacro}[cantidadDeParametros]{Lo que va ser remplazado por el macro} 
\newcommand{\norm}[1]{\vert#1\vert}



\begin{document}

\section{Renombres de Tipos y Estructuras}


\noindent topeDePromocion \textbf{ES}: $\mathbb{R}$

\noindent enum Categoría \textbf{ES} \{Bronce, Plata,Oro, Platino  \}
\vspace{0.5cm}

\noindent Transacciones \textbf{ES} Tuple $\tupla{
        UsuarioOrigen: \Tipo{\Z}, \\
        UsuarioDestino: \Tipo{\Z},
        TipoDeTransaccion: \Tipo{\Z},
        Millas: \Tipo{\R}, \\
        NombrePromo: \Tipo{\str},
        FactorPromo: \Tipo{\R}
    }$

\noindent \comentario{Tipo de transacciones es algo que yo pongo a mano creo, ejemplo 0:Transaccion por referidor, 1: Transferencia, 2: Canjeo de puntos}


\begin{tad}{Airmiles}



    \obs{Usuarios}{\Diccionario{id}{\tupla{MillasTotales: \Tipo{\Z}, MillasDisponiles: \Tipo{\Z}}}}

    \noindent \obs{HistorialDeTransacciones}{\seq{Transacciones}}
    \\
    \texttt{obs} Promociones : dict \{ \\
    \hspace*{1.5em} NombrePromocion: $\str$, \\
    \hspace*{1.5em} $\seq{\Tipo{FactorDePromocion},\Tipo{topeDePromocion}, \Tipo{millasRestantes}}$ \\
    \}

    % ===============Aca comienza la funcion de acumular millas===============

    \vspace{1cm}
    \begin{proc}{AcumularMillas}
        {
            \Inout AM: \Tipo{Airmiles},
            \In idUsuario: \Tipo{\Z},
            \In distancia: \Tipo{\R},
            \In nombrePromo: \Tipo{\str},
        }
        {}
        \requiere{AM = AM0 \land idUsuario \in claves(AM.Usuarios) \land nombrePromo \in claves(AM.Promociones) \land distancia > 0 }
        \aseguraLargo{
            ActualizaMillasUsuario(idUsuario,AM.Usuarios,AM0.Usuarios,\\ distancia, DarFactorCategoria(idUsuario,AM0.Usuarios), \\ DarFactorPromo(nombrePromo,AM0.Promociones)) \yLuego \\
            \\
            EsNuevaTransaccion(AM,AM0,idUsuario,0,0,CalcularMillas(distancia,\\DarFactorCategoria(idUsuario,AM0.Usuarios),\\
            DarFactorPromo(nombrePromo,AM0.Promociones))) \yLuego \\
            \\
            SeConsumioLaPromocion(nombrePromo,AM0.Promociones)
        }
    \end{proc}

    \auxLargo{CalcularMillas}
    {\In distancia: \Tipo{\Z}, \In FactorCategoria: \Tipo{\R}, \In FactorPromo: \Tipo{\R}, idUsuario: \Tipo{\Z}, Usuarios: \Tipo{\Diccionario{id}{\tupla{MillasTotales,MillasDisponibles}}}}
    {\Tipo{\Z}}
    {MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia, \\DarCategoria(idUsuario, Usuarios), nombrePromo,
        Promociones)}

    \vspace{1cm}
    \auxLargo{MillasRestantesDePromocionConTope}
    {\In NombrePromo, \In Promociones}
    {\Tipo{\R}}
    {Promociones \lbrack nombrePromo \rbrack \lbrack 2 \rbrack}

    \vspace{1cm}
    \auxLargo{DarMillasPreviasMasAcumular}
    {
        \In distancia,
        \In FactorCategoria,
        \In FactorPromo,\\
        \In idUsuario,
        \In Usuarios
    }
    {\tupla{\Tipo{\Z},\Tipo{\Z}}}
    {
        \tupla{MillasTotales(Usuarios,idUsuario) + \\
            CalcularMillas(distancia,FactorCategoria,FactorPromo,idUsuario,Usuarios), \\
            MillasDisponibles(Usuarios,idUsuario) + \\
            CalcularMillas(distancia,FactorCategoria,FactorPromo,idUsuario,Usuarios),
        }
    }

    \vspace{1cm}
    \predLargo
    {ActualizaMillasUsuario}
    {
        idUsuario,
        Usuarios,
        Usuarios0, \\
        distancia,
        FactorCategoria,
        FactorPromo
    }
    {Usuarios = SetKey(
            Usuarios0,
            idUsuario, \\
            \tupla{DarMillasPreviasMasAcumular(distancia,FactorCategoria,FactorPromo, \\ idUsuario,Usuarios)_{0}, \\
        DarMillasPreviasMasAcumular(distancia,FactorCategoria,FactorPromo, \\ idUsuario,Usuarios)_{1}}
        )
    }


    \vspace{1cm}
    \auxLargo
    {DarFactorPromo}
    {nombrePromo, Promociones}
    {\Tipo{\R}}
    {
        Promociones[nombrePromo][0]
    }

    \vspace{1cm}
    \auxLargo
    {topeDePromocion}
    {nombrePromo, Promociones}
    {\Tipo{\R}}
    {Promociones[nombrePromo][1]}

    \vspace{1cm}
    \auxLargo
    {MillasRestantesDePromo}
    {nombrePromo, Promociones}
    {\Tipo{\R}}
    {
        Promociones[nombrePromo][2]
    }



    \vspace{1cm}
    \auxLargo
    {MillasQueGanariaElUsuario}
    {idUsuario, Usuarios, distancia, categoria, \\ nombrePromo, Promociones}
    {\Tipo{\R}}
    {
        distancia * DarCategoria(idUsuario,Usuarios) \\
        * DarFactorPromo(nombrePromo,Promociones)
    }

    \vspace{1cm}

    \comentarioLargo{
        Chequea si la promocion se consumio,en caso de que no se haya consumido
        actualiza en el observador la cantidad de millas restantes de la promocion
    }

    \predLargo
    {SeConsumioLaPromocion}
    {
        idUsuario,
        Usuarios,
        distancia,
        categoria,
        nombrePromo, \\
        Promociones,
        Promociones0
    }
    {
        MillasRestantesDePromo(nombrePromo, Promociones0) \leq \\
        MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia, categoria, \\ nombrePromo, Promociones)
        \implica
        Promociones = DelKey(Promociones0, nombrePromo)
        \land
        MillasRestantesDePromo(nombrePromo, Promociones0) > \\
        MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia, categoria, \\
        nombrePromo, Promociones)
        \implica \\
        Promociones = SetKey(
        Promociones0,
        nombrePromo, \\
        \lbrack
        DarFactorPromo(nombrePromo,Promociones0), \\
        topeDePromocion(nombrePromo,Promociones0), \\
        MillasRestantesDePromo(nombrePromo,Promociones0)- \\
        CalcularMillas(
        distancia,
        DarFactorCategoria(idUsuario, Usuarios), \\
        DarFactorPromo(nombrePromo, Promociones),
        idUsuario,
        Usuarios
        )
        \rbrack
        )
    }

    % MillasRestantesDePromo(nombrePromo,Promociones0) > MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia,categoria, \\ nombrePromo, Promociones)  -> Promociones = SetKey(Promociones0,nombrePromo, [Promociones0[nombrePromo][0],Promociones0[nombrePromo][1],Promociones0[nombrePromo][2] - CalcularMillas(distancia :  DarFactorCategoria(idUsuario,Usuarios),DarFactorPromo(nombrePromo, Promociones) , idUsuario, Usuarios)])


    % pred SeConsumioLaPromocion(idUsuario,Usuarios, distancia, categoria nombrePromo,Promociones,Promociones0){
    % 	topeDePromocion(Promocion,NombrePromocion) <= MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia,categoria,nombrePromo, Promociones)  => Promociones = DelKey(Promociones0,nombrePromocion) 
    % }


    % Aux MillasRestanteDePromo(nombrePromo,Promociones) = Promociones[nombrePromo][2] 

    % Aux MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia,categoria,nombrePromo, Promociones) = distancia* DarCategoria(idUsuario,Usuarios) * darFactorPromo(nombrePromo,Promociones) 

    % Aux topeDePromocion(Promocion,NombrePromocion) = Promociones[NombrePromocion][1] 

    % pred SubioCategoria(idUsuario,Usuarios,Usuarios0){
    % 	DarCategoria(idUsuario, Usuarios) != DarCategoria(idUsuario, Usuarios0)
    % }

    % pred ActualizaMillasUsuario(idUsuario, Usuarios, Usuarios0,distancia,FactorCategoria,FactorPromo){
    % 	Usuarios[idUsuario] = SetKey(AM0.Usuarios,idUsuario,⟨DarMillasPreviasMasAcumular(distancia,FactorCategoria,FactorPromo,idUsuario, 	Usuarios:dict{})[0],DarMillasPreviasMasAcumular(distancia,FactorCategoria,FactorPromo,idUsuario, Usuarios:dict{})[1]⟩)
    % }

    % Aux DarMillasPreviasMasAcumular(distancia,FactorCategoria,FactorPromo,idUsuario, Usuarios:dict{}): Tupla = ⟨MillasTotales(Usuarios, idUsuario) + CalcularMillas(distancia,FactorCategoria,FactorPromo,idUsuario, Usuarios),
    %  MillasDisponibles(Usuarios, idUsuario) + CalcularMillas(distancia,FactorCategoria,FactorPromo,idUsuario, Usuarios)⟩ 

    % aux ExistePromoYTieneTope(nombrePromo,Promociones): bool = (nombrePromo ∈ Promociones) ∧L topeDePromocion(Promocion,NombrePromocion) != 0


    % aux MillasRestantesDePromocionConTope(nombrePromo,Promociones) = Promociones[nombrePromo][2]
    % aux darFactorPromo(nombrePromo,Promociones) = IfThenElse(ExistePromoYTieneTope(nombrePromo,Promociones), topeDePromocion(Promocion,NombrePromocion), 1 ) // Si existe la promo su valor es, lo que vale la promo, si no exsite es por default 1 "NoPromo" que esta siempre en el sistema cuando se crea. Capaz hay que revisar si no necesita otro bucle anidado!

    % Aux CalcularMillas(distancia,FactorCategoria,FactorPromo,idUsuario, Usuarios) = IfThenElse(MillasRestantesDePromocionConTope(nombrePromo,Promociones) >= MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia,categoria,nombrePromo, Promociones),MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia,categoria,nombrePromo, Promociones),
    % MillasQueGanariaElUsuario(idUsuario, Usuarios, DistanciaRestanteDePromo(nombrePromo,Promociones),categoria,nombrePromo, Promociones) + MillasQueGanariaElUsuario(idUsuario, Usuarios, distancia - DistanciaRestanteDePromo(nombrePromo,Promociones) ,categoria,"NoPromo", Promociones)
    % ) 



\end{tad}

\end{document}
% Hay que chequear varias cosas:
% proc AcumularMillas(inout AM: Airmiles, in: idUsuario:Z, in: distancia:R, in: nombrePromo:str){
% Requiere{ AM = AM0 ∧ idUsuario ∈   claves(AM.Usuarios) ∧ nombrePromo ∈ claves(AM.Promociones)}
% Asegura{

% ActualizaMillasUsuario(idUsuario, Usuarios, Usuarios0,distancia,FactorCategoria,FactorPromo) ∧L

% SubioCategoria(idUsuario,Usuarios) ∧ 

% EsNuevaTransaccion(AM, AM0, idUsuario, idReferidor, TipoDeTransaccion, CalcularMillas(distancia,FactorCategoria,FactorPromo,idUsuario, Usuarios),
% NombrePromo, FactorPromo)} ∧L

% SeConsumioLaPromocion(nombrePromo,Promociones)

% }